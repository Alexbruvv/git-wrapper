// TODO (phase 2): resolve the nearest .gitwrapper and print the account, host
// and config path that would be used for the current directory.
export async function which(): Promise<number> {
  process.stderr.write("gw which: not implemented yet (phase 2)\n");
  return 1;
}
