// TODO (phase 4): interactively scaffold a .gitwrapper, offering the logged-in
// gh accounts to choose from.
export async function init(): Promise<number> {
  process.stderr.write("gw init: not implemented yet (phase 4)\n");
  return 1;
}
