// TODO (phase 4): check git/gh install, print gh auth status + accounts,
// validate the nearest .gitwrapper, and report any problems.
export async function doctor(): Promise<number> {
  process.stderr.write("gw doctor: not implemented yet (phase 4)\n");
  return 1;
}
