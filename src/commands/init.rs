// TODO(phase 4): scaffold a .gitwrapper (interactive picker on a TTY) and add
// it to .gitignore unless --no-gitignore is passed.
pub fn init(_args: &[String]) -> i32 {
    eprintln!("gw init: not implemented yet (phase 4)");
    1
}
